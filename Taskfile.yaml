version: "3"

dotenv: [".env"]

tasks:
  demo:
    - uv run python -m demo.app

  prepare-dataset:
    - uv run python -m scripts.build_polyphony_pairs --max-songs 10

  train:
    - uv run python -m seed_vc.train.train --dataset data/polyphony-project-dataset/pairs.csv
