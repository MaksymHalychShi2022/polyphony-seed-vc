# Airflow requirements for CI DAG integrity tests and local orchestration.
# Kept separate from pyproject.toml to avoid transitive dependency conflicts
# between Airflow's pinned packages and the ML stack.
#
# Requires Python >=3.10,<3.12.
#
# Install in CI with:
#   pip install apache-airflow==2.8.4 \
#     --constraint https://raw.githubusercontent.com/apache/airflow/constraints-2.8.4/constraints-3.11.txt
apache-airflow==2.8.4
