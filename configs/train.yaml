# @package _global_

defaults:
  - _self_
  - model: default
  - trainer: default
  - optimizer: default
  - logger: mlflow
  - paths: default
  - hparams_search: null
  - experiment: null

run_name: "my_run"
pretrained_ckpt: null
pretrained_model: "DiT_seed_v2_uvit_whisper_base_f0_44k_bigvgan_pruned_ft_ema.pth"
seed: null

hydra:
  run:
    dir: "${oc.env:EXPERIMENTS_DIR,runs}/${run_name}_${now:%Y-%m-%d_%H-%M-%S}"
  job:
    chdir: false
